@.genai/skills/README.md

Cursor skill wrappers — invoke via `@skill-name`. Each `SKILL.md` references the canonical skill under `.genai/skills/`. Language and stack conventions are handled by glob-scoped rules instead (see `.cursor/rules/README.md`).
